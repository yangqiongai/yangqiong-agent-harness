# 安全政策

泱穹智能体框架（Yangqiong Agent Harness）的安全政策：受支持版本、漏洞报告渠道与响应承诺、安全模型与默认安全行为。

## 受支持的版本

| 版本 | 支持状态 |
|---|---|
| 1.1.x | ✅ 支持中（当前迭代） |
| 1.0.x | ⚠️ 仅接收关键安全修复 |
| < 1.0 | ❌ 不再支持 |

## 报告漏洞

**请勿通过公开 Issue 报告安全漏洞。**

推荐通过 GitHub 私密漏洞报告（仓库页 Security → Report a vulnerability）提交，报告时请尽量包含：

- 受影响的版本与模块
- 漏洞类型与影响评估（可获取的权限、数据泄露范围等）
- 复现步骤或概念验证代码（PoC）

### 响应承诺

| 阶段 | 时限 |
|---|---|
| 确认收到报告 | 48 小时内 |
| 完成评估与定级 | 7 天内 |
| 修复与发布 | 按定级排期，严重级别优先 |

采用**协调披露**：与报告者确认修复方案后，随新版本发布统一披露漏洞详情与影响范围，不提前公开未修复漏洞。

## 安全模型与信任边界

框架按以下信任边界设计，超出边界的场景需使用者自行加固：

- **模型输出不可信**：模型返回内容视为外部输入，经注入检测与内容审查策略处理，不直接作为可信指令执行
- **工具执行受控**：所有工具调用经权限引擎与策略门评估（黑名单 > 白名单 > 权限引擎 > 默认拒绝），判定记录写入审计
- **文件访问受沙箱约束**：本地文件工具的路径经词法规范化与真实路径双重校验，防止软链/越界逃逸
- **Shell 命令分级审批**：本地 Shell 命令先做词法解析（引号、变量展开、命令替换还原）再按只读放行 / 危险拒绝 / 不确定转人工确认分级

## 默认安全行为

开箱即用的默认能力，使用者无需额外配置即生效：

| 能力 | 默认行为 |
|---|---|
| 权限模式 | DEFAULT 模式采用白名单语义：存在 allow 规则时未命中即拒绝，无 allow 规则时默认放行 |
| 审计 | 策略门判定记录写入有界环形缓冲（保留最近 5000 条），可替换为持久化审计出口 |
| 注入检测 | 覆盖高熵内容、同形词混淆、Base64 编码指令、重复填充等启发式规则 |
| 工具护栏 | 工具参数与结果统一过护栏规则链，支持 BLOCK 拦截与 MASK 脱敏 |
| 文件沙箱 | 词法校验（normalize + 前缀边界）与真实路径校验双重防护 |
| Shell 审批 | 词法解析后分级：只读命令放行、危险命令拒绝、无法归类的命令转人工确认 |
| 预算控制 | Token 与成本预算策略超限时告警或中止 |

## 反馈边界

以下情况**不属于**安全漏洞，请通过公开 Issue 反馈：

- 依赖的第三方组件问题（请同时向对应上游报告）
- 需要 specific 部署环境配合才成立的理论风险
- 未开启沙箱/审批等可选防护时的预期行为
